Let $f$ be a function mapping from a set $A$ onto a set $B$. 

The image of this function are all the elements in set $B$ which have been mapped onto, so any $b$ in $B$ such that $b=f(a)$ for some $a$ in $A$.
